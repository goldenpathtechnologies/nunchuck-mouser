name: Release

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"
      - "v[0-9]+.[0-9]+.[0-9]+-[a-zA-Z]+.[0-9]+"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TBD"
  release:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - run: echo "TBD"